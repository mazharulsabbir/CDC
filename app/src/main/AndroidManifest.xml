<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="edu.daffodil.cdc">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".MainActivity"
            android:theme="@style/dashboard" />
        <activity android:name=".AppointmentScheduling" />
        <activity
            android:name=".SelfAssessment"
            android:theme="@style/dashboard" />
        <activity android:name=".ContactUs" />
        <activity android:name=".Feedback" />
        <activity android:name=".JobNotice" />
        <activity
            android:name=".CareerPlanningMap"
            android:theme="@style/dashboard" />
        <activity android:name=".UsefulLinks" />
        <activity android:name=".Announcements" />
        <activity android:name=".CareerOptionActivity" />
        <activity
            android:theme="@style/AppTheme.SplashScreen"
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
