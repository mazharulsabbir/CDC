<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="edu.daffodil.cdc">

    <dist:module dist:instant="true" />
    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".JobNotice"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".SelfAssessment"
            android:theme="@style/dashboard" />
        <activity
            android:name=".ContactUs"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Feedback"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".CareerPlanningMap"
            android:theme="@style/dashboard" />
        <activity
            android:name=".UsefulLinks"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".Announcements"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".CareerOptionActivity"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".MainActivity"
            android:theme="@style/dashboard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>